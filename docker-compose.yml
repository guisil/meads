services:
  postgres:
    image: postgres:18-alpine
    container_name: meads-postgres
    environment:
      POSTGRES_DB: meads
      POSTGRES_USER: meads
      POSTGRES_PASSWORD: meads
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
